name: Build and Release

on:
  push:
    branches: ["saga"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

env:
  CNAME: world.thingy.rocks
  SENTRY_DSN: https://f0636caa395147fcb07f781d0c1e8e6c@o4504255385174016.ingest.sentry.io/4504255391596544
  # For uploading sourcemaps
  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
  # EU
  WEBSOCKET_ENDPOINT: wss://ouqkjmrbb8.execute-api.eu-central-1.amazonaws.com/2022-11-22
  MAP_API_KEY: v1.public.eyJqdGkiOiJjMmY3MWFiMC04YjQ2LTQ4MzQtOTVkMS0zNzMxZWFkNzIyYTYifRtWGf9E2EE6gHZk0L2le1atLNN2t8txL5EmjCV-Dt3ZWFWfqrodtHYXcBG1F6DzOUHe5mpApSvJ5U8sdKoVZzFsANTkB_Ok6-c7ZiMty8qiRV0UjyiLTxeTmLNTyAAJMII5U3_3jXOvK9qJrBSr2hj23QTzYDvDeXui6XQWgSFsxTutndOu9cwpOqS8lWco6OQDR8Nii4_ansVRbQH9oaBzhzVrJ1E48xY9Gm8sBH2IqUIlBCuy4r82TwC0vwbbGsuvTc-trv8Db2gz7ETh7EV34bZwltbLvX6fNhQ7rQp5PJRpKXPL1_tPHA7251tLIWIW8GbjzrxHUvLGNgwJSBQ.Y2MzMTcwNWYtZTgwMy00NGZlLWFiNGItNzVkMDFmMDM0Nzcy
  COGNITO_USER_POOL_CLIENT_ID: 1qi8v6el2p71ua54r6ainf8i0a
  COGNITO_USER_POOL_URL: https://cognito-idp.eu-central-1.amazonaws.com/eu-central-1_pkaPxL9n1
  COGNITO_IDENTITY_POOL_ID: eu-central-1:6e58f887-ea36-477b-a523-d31093e585b9
  COGNITO_DOMAIN_URL: https://thingy-rocks-374216331074.auth.eu-central-1.amazoncognito.com
  STREAM_METADATA_TABLE: video-streaming-StreamMetadataD1FE2960-1C369PH9SUBY9
  # US
  # WEBSOCKET_ENDPOINT: wss://ui1r9dn0sk.execute-api.us-west-2.amazonaws.com/2022-11-22

jobs:
  next-release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.nextRelease }}

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v3
        with:
          node-version: "22.x"
          cache: "npm"
      - name: Get current release
        run:
          echo "currentRelease=$(git describe --tags --abbrev=0)" >> $GITHUB_ENV

      - name: Determine next release version
        uses: nordicsemiconductor/cloud-get-next-version-action@saga
        id: version
        with:
          branch: saga
          defaultVersion: "${{ env.currentRelease }}-${{ github.sha }}"

  main:
    runs-on: ubuntu-latest

    needs: [next-release]

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: "22.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --no-audit

      - run: npm test

      - name: Build
        env:
          VERSION: ${{ needs.next-release.outputs.version }}
        run: |
          npm run build
          cp -r ./static ./build
          cp -r node_modules/svg-country-flags/svg/ ./build/static/flags
          mkdir ./build/.well-known
          echo ${{ needs.next-release.outputs.version }} > ./build/.well-known/release

      - name: Deploy to CloudFlare pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run:
          npx wrangler pages deploy --project-name thingy-world --branch saga
          --commit-hash ${{ github.sha }} ./build

      - name: Semantic release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
        run: npx semantic-release
