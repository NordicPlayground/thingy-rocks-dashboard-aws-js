export WEBSOCKET_ENDPOINT=...
export MAP_API_KEY=...
export COGNITO_USER_POOL_URL=...
export COGNITO_USER_POOL_CLIENT_ID=...
# For guest/unauthenticated AWS credentials when user is not logged in
export COGNITO_IDENTITY_POOL_ID=...
# For Cognito Hosted UI (managed login) - get from CDK output cognitoDomainUrl
export COGNITO_DOMAIN_URL=https://thingy-rocks-ACCOUNT.auth.REGION.amazoncognito.com
# Redirect URL after sign-in/sign-out (default: http://localhost:8080/)
# export COGNITO_REDIRECT_URL=http://localhost:8080/
# Configure if you want to track client side errors
# export SENTRY_DSN=...
export AWS_REGION=
export STREAM_METADATA_TABLE=...